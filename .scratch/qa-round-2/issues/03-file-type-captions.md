# 03: File-type captions on all file buttons

**What to build:** Every file button shows a small muted caption naming the file type: Import snapshot / Export snapshot (`.json`), actuals import (`.csv`), business-case export (`.html`). Each file picker's accepted types match its caption.

**Blocked by:** None (can start immediately)

**Status:** done (8a3fe62)

- [x] Snapshot import and export buttons show `.json`
- [x] Actuals import shows `.csv`
- [x] Business-case export shows `.html`
- [x] File input `accept` attributes match the captions
- [x] Captions are part of the button's accessible name or otherwise readable by screen readers
